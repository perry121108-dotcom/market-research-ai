# QA 日誌 - 2026-05-13 第二輪

## 一、QA 基本資訊

- QA 日期：2026-05-13
- QA 人員/角色：專案 QA / Claude 修正後查驗
- 專案階段：MVP 開發測試階段
- 審查範圍：Claude 依第一輪 QA 表修正後的工作區
- 審查基準：`docs/QA_RULES.md`
- 最終判定：`Pass with Risks`

## 二、檢測命令

本次 QA 實際執行：

```bash
git status --short
git log -1 --stat --oneline
git ls-files .env .env.local .env.* key.txt
git ls-files .next node_modules out dist build
rg -n "dangerouslySetInnerHTML|eval\(|new Function|innerHTML|outerHTML" src
npm run build
npm run lint
rg --hidden -n "API_KEY|SECRET|TOKEN|PASSWORD|AIza|sk-" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' -g '!*.bak' .
git status --short --ignored
git check-ignore -v .claude\worktrees\mystifying-zhukovsky-f105dd\.env.local .claude\worktrees\mystifying-zhukovsky-f105dd\key.txt .claude\worktrees\mystifying-zhukovsky-f105dd\src\app\api\research\route.ts .claude\worktrees\mystifying-zhukovsky-f105dd\package.json
```

結果摘要：

- `npm run build`：Pass
- `npm run lint`：Fail / Blocked，因 `next lint` 進入互動式 ESLint 初始化提示
- `git ls-files .env .env.local .env.* key.txt`：無輸出
- `git ls-files .next node_modules out dist build`：0
- `rg ... src`：仍找到 `ReportView.tsx` 使用 `dangerouslySetInnerHTML`，但目前是 `DOMPurify.sanitize(marked(report))` 後渲染
- `rg --hidden ...`：在 `.claude/worktrees/.../.env.local` 與 `.claude/worktrees/.../key.txt` 找到真實 API key 形態內容

## 三、總體結論

Claude 已完成第一輪中最關鍵的 git tracking 修正：`.env.local`、`key.txt`、`.next/`、`node_modules/` 已不再被 git tracking，且 build 可通過。

但 QA 發現新的交付風險：專案根目錄存在 `.claude/` 工具 worktree，裡面有另一份專案副本、生成檔、依賴檔與 API key 副本。雖然其中 `.env.local` 與 `key.txt` 被目前 `.gitignore` 規則 ignore，但 `.claude/` 目錄本身仍是 untracked，且部分非 ignored 檔案可能在 `git add .` 時被加入。

因此本輪判定為 `Pass with Risks`。若要安全 push，必須先處理 `QA2-001` 與 `QA2-002`。

## 四、修改項目表

| ID | 優先級 | 類型 | 檔案/範圍 | 證據 | 問題 | 要求修改 | 驗收標準 | 狀態 |
|---|---|---|---|---|---|---|---|---|
| QA-001 | P0 | Security | `.env.local`, `key.txt` | `git ls-files .env .env.local .env.* key.txt` 無輸出 | 原本 secret tracking 已修正 | 確認舊 key 已 rotate；不得把真實 key commit | git tracking 無 secret；使用者確認舊 key 已停用 | Verified |
| QA-002 | P1 | Maintainability | `.next/`, `node_modules/` | `git ls-files .next node_modules out dist build` 回傳 0 | 原本生成檔與依賴目錄 tracking 已修正 | 保持 `.gitignore` 規則 | `git ls-files .next node_modules out dist build` 為 0 | Verified |
| QA-003 | P1 | Security | `src/components/ReportView.tsx` | `ReportView.tsx` 使用 `DOMPurify.sanitize(marked(report) as string)` 後才進 `dangerouslySetInnerHTML` | XSS 風險已降低，但仍依賴 sanitizer 流程 | 保留 sanitizer；後續補 XSS regression 測試 | `<script>`、`onerror`、`javascript:` 不得執行 JS | Fixed |
| QA-004 | P2 | Correctness | `src/app/api/research/route.ts` | 已加入 JSON try/catch、object 檢查、string 檢查、長度限制 | API input validation 已補強 | 後續可補 API route 單元測試 | malformed JSON、空值、非字串、過長輸入回 400 | Fixed |
| QA-005 | P2 | Correctness | `src/app/api/research/route.ts` | 已改為 `const apiKey = process.env.GEMINI_API_KEY` 並明確檢查 | env 非空斷言已移除 | 維持不洩漏敏感資訊的錯誤訊息 | 缺 env 時回 500 且不印出 key | Fixed |
| QA-008 | P2 | Project Hygiene | `.env.example` | `.env.example` 存在且只含 placeholder | 新環境設定範例已補 | 保持不含真實 key | `.env.example` 不含真實 key | Verified |
| QA2-001 | P0 | Security | `.claude/worktrees/.../.env.local`, `.claude/worktrees/.../key.txt` | `rg --hidden ...` 在 `.claude/worktrees/...` 找到真實 API key 形態內容 | 專案內仍存在 secret 副本，即使目前被 ignore，也不應留在交付資料夾內 | 刪除 `.claude/worktrees/.../.env.local` 與 `key.txt`，或整個移除 `.claude/` 工具目錄；確認舊 key 已 rotate | `rg --hidden -n "AIza|GEMINI_API_KEY" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' .` 不再顯示真實 key，只允許 `.env.example` placeholder 與程式 env 讀取 | Open |
| QA2-002 | P1 | Maintainability | `.claude/` | `git status --short --ignored` 顯示 `?? .claude/`；`git check-ignore` 未顯示 `.claude/.../src` 與 `.claude/.../package.json` 被 ignore | `.claude/` 是工具 worktree，不應在專案 repository 內被加入，也可能混入重複 source 與生成檔 | 將 `.claude/` 加入 `.gitignore`，或移出專案根目錄 | `git status --short --ignored` 不再顯示 `?? .claude/`；`git check-ignore -v .claude/.../package.json` 有 `.claude/` 規則命中 | Open |
| QA2-003 | P2 | Build / QA | `package.json` lint script | `npm run lint` 進入 Next.js ESLint 互動式初始化提示並以 exit code 1 結束 | lint script 不適合 CI/QA 非互動驗證 | 設定 ESLint config，或調整 lint script 讓它非互動執行 | `npm run lint` 非互動通過或產出明確 lint errors | Open |
| QA2-004 | P2 | Maintainability | `setup.js` | `rg` 顯示 `setup.js` 仍有 `process.env.GEMINI_API_KEY!` | setup script 仍保留舊式非空斷言，可能重建出舊風險模式 | 若此檔仍使用，更新 env 檢查；若不用，移除或標記廢棄 | `rg -n "GEMINI_API_KEY!" setup.js` 無輸出，或文件明確說明不再使用 | Open |
| QA2-005 | P2 | Product Quality | `src/app/api/research/route.ts`, `src/components/ReportView.tsx` | API 仍只回傳 `report, chartData`，未回傳 grounding source metadata | 第一輪 Deferred 項仍未完成 | 上線前補 sources 顯示 | 報告頁可顯示來源；無來源時 UI 不崩潰 | Deferred |
| QA2-006 | P2 | Maintainability | `package.json`, `src/app/api/research/route.ts` | 仍使用 `@google/generative-ai` | 第一輪 Deferred 的 SDK 遷移未完成 | 評估遷移 `@google/genai` | build 通過且 Search grounding 正常 | Deferred |

## 五、Claude 建議執行順序

1. 先處理 `QA2-001`：移除 `.claude/worktrees/...` 裡的真實 secret 副本，並確認舊 key 已 rotate。
2. 處理 `QA2-002`：將 `.claude/` 加入 `.gitignore`，或把 `.claude/` 移出專案根目錄。
3. 處理 `QA2-003`：補 ESLint config，讓 `npm run lint` 可非互動執行。
4. 處理 `QA2-004`：修正或移除 `setup.js` 中舊式 `GEMINI_API_KEY!`。
5. 保留 `QA2-005`、`QA2-006` 為上線前項目。

## 六、驗收命令

Claude 修完後必須回報：

```bash
git status --short --ignored
git ls-files .env .env.local .env.* key.txt .next node_modules out dist build
rg --hidden -n "AIza|GEMINI_API_KEY" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' .
git check-ignore -v .claude/worktrees/mystifying-zhukovsky-f105dd/package.json
npm run build
npm run lint
```

## 七、剩餘風險

- `@google/generative-ai` SDK 遷移仍 Deferred。
- Grounding sources 顯示仍 Deferred。
- XSS 已加入 DOMPurify，但尚未有自動化 regression test。
- `.env.local` 根目錄仍存在作為本機執行使用，這可接受，但不可 commit，且舊 key 必須 rotate。

## 八、QA 結論

本輪 QA 判定：`Pass with Risks`。  
已通過 build 與主要 tracking 驗收，但不可直接 `git add .` / push，必須先處理 `.claude/` 目錄與其中 secret 副本。
