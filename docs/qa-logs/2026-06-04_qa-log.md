# QA 日誌 - 2026-06-04

## QA 範圍
- QA 類型：產品正確性 / 可維護性 / 報表可判讀性
- 專案路徑：D:\market-research-ai
- 修改目標：固定市場需求熱度標準、分離用途適配判斷、競品必須提供可查證連結。

## 修改項目表
| ID | 優先級 | 類型 | 問題 | 修改內容 | 驗證方式 | 狀態 |
|---|---|---|---|---|---|---|
| QA4-001 | P1 | Product Correctness | 市場熱度可能隨調研用途改變，使用者會誤判同一商品在不同用途下的需求。 | 在 prompt 強制定義 market_demand_heat_v1，trendScore 固定以搜尋需求、通路能見度、評論討論、競品活動加權，不隨 purpose 改變。 | npm run build；人工檢查 prompt 規則。 | Fixed |
| QA4-002 | P1 | Product Correctness | 選品熱度高但開發建議低時，缺少明確判讀規則。 | 新增 purposeFitScore、saturationScore、developmentRecommendation，前端新增「市場熱度與用途判讀」區塊。 | npm run build；檢查 ReportView 型別與渲染。 | Fixed |
| QA4-003 | P1 | Grounding | 首推競品只有品牌名，缺少網址、型號與依據。 | competitors schema 新增 productName、modelName、officialUrl、marketplaceUrl、evidenceUrls、evidenceSummary、whyRecommended；前端首推競品優先選有連結者並可點擊。 | npm run build；檢查 KpiCards / CompetitorChart。 | Fixed |
| QA4-004 | P2 | Maintainability | 模型若漏給新欄位，前端可能拿到不完整 chartData。 | API 新增 normalizeChartData，補 trendBasis 預設值並正規化 competitors/evidenceUrls/分數。 | npm run build。 | Fixed |

## QA 結論
本輪已完成產品判讀邏輯修正。市場熱度現在是固定需求指標；用途只影響用途適配、飽和度解讀與開發建議；首推競品必須附可查證連結，否則前端會標示需人工確認。

## 待驗證
- 需用實際部署環境輸入同一商品、不同調研用途，確認 trendScore 不因 purpose 改變。
- 若 Gemini 回傳的競品仍缺網址，應要求 Claude/模型調整搜尋 grounding 或在報告中降級為待確認競品。

## 驗證結果
- `npm run build`：Pass，Next.js production build compiled successfully，TypeScript validity check passed。
- `npm run lint`：Pass，No ESLint warnings or errors。
- `git ls-files .env .env.local .env.* key.txt .next node_modules out dist build`：僅顯示 `.env.example`，未追蹤 `.env.local`、`key.txt`、`.next/`、`node_modules/`。
- `rg --hidden -n "AIza|GEMINI_API_KEY|sk-[A-Za-z0-9]" ...`：未在程式碼中找到實際 key；僅有 `.env.example`、README/QA 文件文字說明與 `process.env.GEMINI_API_KEY` 讀取。
- `rg "market_demand_heat_v1|purposeFitScore|saturationScore|evidenceUrls|首推競品|市場熱度與用途判讀"`：已確認 prompt、API、前端與本 QA log 都有對應欄位與規則。

- 本機 dev server：啟動於 `http://127.0.0.1:3100` 後，`Invoke-WebRequest` 回傳 HTTP 200；檢查後已停止 dev server。
- in-app browser 視覺 QA：Blocked，Browser runtime 發生 Windows sandbox 初始化錯誤，未完成截圖/互動檢查；需由人工或 Claude 在可用瀏覽器環境補測。

## 最新規則入口
- `RULES.md` 已更新 Current latest QA log 與 Claude Task Source，指向 `docs/qa-logs/2026-06-04_qa-log.md`。
