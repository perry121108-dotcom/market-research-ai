# QA 日誌 - 2026-05-13 第三輪

## 一、QA 基本資訊

- QA 日期：2026-05-13
- QA 人員/角色：專案 QA / 第三輪查驗
- 專案階段：MVP 開發測試階段
- 審查範圍：第二輪 QA 後的目前工作區
- 審查基準：`docs/QA_RULES.md`
- 最終判定：`Fail`

## 二、檢測命令

本次 QA 實際執行：

```bash
git status --short --ignored
git log -1 --stat --oneline
git ls-files .env .env.local .env.* key.txt .next node_modules out dist build
rg --hidden -n "AIza|GEMINI_API_KEY" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' .
git check-ignore -v .claude\worktrees\mystifying-zhukovsky-f105dd\package.json .claude\worktrees\mystifying-zhukovsky-f105dd\src\app\api\research\route.ts .claude\worktrees\mystifying-zhukovsky-f105dd\.env.local .claude\worktrees\mystifying-zhukovsky-f105dd\key.txt
rg -n "dangerouslySetInnerHTML|eval\(|new Function|innerHTML|outerHTML" src
rg -n "GEMINI_API_KEY!|process\.env\.GEMINI_API_KEY" setup.js src\app\api\research\route.ts
npm run build
npm run lint
```

結果摘要：

- `npm run build`：Pass
- `npm run lint`：Fail / Blocked，仍進入 Next.js ESLint 互動式初始化提示
- `git ls-files .env .env.local .env.* key.txt .next node_modules out dist build`：無輸出
- `rg --hidden ...`：仍在 `.claude/worktrees/.../.env.local` 與 `.claude/worktrees/.../key.txt` 找到真實 API key 形態內容
- `git check-ignore -v ...`：只看到 `.env.local` 與 `key.txt` 被現有規則命中；`.claude/.../package.json` 與 `.claude/.../src/.../route.ts` 未被 `.claude/` 規則 ignore
- `rg ... src`：仍有 `dangerouslySetInnerHTML`，但目前路徑是 `DOMPurify.sanitize(marked(report))` 後渲染
- `setup.js`：仍有 `process.env.GEMINI_API_KEY!`

## 三、總體結論

第二輪 QA 指出的最高風險仍未解除：

1. `.claude/worktrees/...` 內仍存在 secret 副本。
2. `.claude/` 仍未被整體 ignore，工具 worktree 仍可能被 `git add .` 帶入。
3. `npm run lint` 仍無法非互動執行。
4. `setup.js` 仍保留舊式 `GEMINI_API_KEY!` 非空斷言。

雖然 build 通過，且原本 tracked 的 `.env.local`、`key.txt`、`.next/`、`node_modules/` 已從 git tracking 移除，但根據 `docs/QA_RULES.md`，專案資料夾內仍存在真實 secret 副本時不可判定為 Pass 或可安全 push。

本輪 QA 判定：`Fail`。

## 四、修改項目表

| ID | 優先級 | 類型 | 檔案/範圍 | 證據 | 問題 | 要求修改 | 驗收標準 | 狀態 |
|---|---|---|---|---|---|---|---|---|
| QA3-001 | P0 | Security | `.claude/worktrees/.../.env.local`, `.claude/worktrees/.../key.txt` | `rg --hidden -n "AIza|GEMINI_API_KEY" ...` 仍在 `.claude/worktrees/...` 找到真實 key 形態內容 | 專案資料夾內仍有 secret 副本 | 刪除 `.claude/worktrees/.../.env.local`、`.claude/worktrees/.../key.txt`，或整個移除 `.claude/` 工具目錄；確認舊 key 已 rotate | `rg --hidden -n "AIza|GEMINI_API_KEY" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' .` 不再顯示真實 key，只允許 `.env.example` placeholder 與程式 env 讀取 | Open |
| QA3-002 | P1 | Maintainability | `.claude/` | `git status --short --ignored` 仍顯示 `?? .claude/`；`git check-ignore` 未顯示 `.claude/.../package.json` 被 `.claude/` 規則命中 | Claude 工具 worktree 未被整體 ignore，可能被誤加進 repo | 在 `.gitignore` 加入 `.claude/`，或將 `.claude/` 移出專案根目錄 | `git status --short --ignored` 不再顯示 `?? .claude/`；`git check-ignore -v .claude/worktrees/mystifying-zhukovsky-f105dd/package.json` 命中 `.claude/` 規則 | Open |
| QA3-003 | P2 | Build / QA | `package.json` / ESLint config | `npm run lint` 仍進入互動式 ESLint 初始化，exit code 1 | lint 無法作為非互動 QA/CI 驗證命令 | 新增 ESLint 設定，或調整 lint script | `npm run lint` 非互動通過或產出明確 lint errors | Open |
| QA3-004 | P2 | Maintainability | `setup.js` | `rg -n "GEMINI_API_KEY!" setup.js` 仍有輸出 | setup script 仍保留舊式非空斷言，可能重建舊風險模式 | 修正 env 檢查或移除/廢棄 `setup.js` | `rg -n "GEMINI_API_KEY!" setup.js` 無輸出，或文件明確說明不再使用 | Open |
| QA3-005 | P1 | Security | `src/components/ReportView.tsx` | `ReportView.tsx` 使用 `DOMPurify.sanitize(marked(report) as string)` 後進 `dangerouslySetInnerHTML` | 原始 XSS 風險已降低，但尚未有 regression test | 保留 sanitizer，後續補 XSS 測試 | `<script>`、`onerror`、`javascript:` 不得執行 JS | Fixed |
| QA3-006 | P2 | Correctness | `src/app/api/research/route.ts` | 已有 JSON try/catch、object 檢查、string 檢查、長度限制、env 檢查 | API validation 維持修正狀態 | 後續補 API route 測試 | malformed JSON、空值、非字串、過長輸入回 400 | Fixed |
| QA3-007 | P2 | Product Quality | `src/app/api/research/route.ts`, `src/components/ReportView.tsx` | API 仍只回傳 `report, chartData`，未回傳 grounding source metadata | Grounding sources 顯示仍未完成 | 上線前補 sources 顯示 | 報告頁可顯示來源；無來源時 UI 不崩潰 | Deferred |
| QA3-008 | P2 | Maintainability | `package.json`, `src/app/api/research/route.ts` | 仍使用 `@google/generative-ai` | SDK 遷移仍未完成 | 評估遷移 `@google/genai` | build 通過且 Search grounding 正常 | Deferred |

## 五、Claude 建議執行順序

1. 立即處理 `QA3-001`：移除 `.claude/worktrees/...` 中所有 secret 副本，並確認舊 key 已 rotate。
2. 處理 `QA3-002`：將 `.claude/` 加入 `.gitignore`，或將 `.claude/` 移出 repo root。
3. 處理 `QA3-003`：補 ESLint config，讓 `npm run lint` 可非互動執行。
4. 處理 `QA3-004`：修正或移除 `setup.js` 中舊式 `GEMINI_API_KEY!`。
5. 再跑驗收命令並產出修正回報。

## 六、驗收命令

Claude 修完後必須回報：

```bash
git status --short --ignored
git ls-files .env .env.local .env.* key.txt .next node_modules out dist build
rg --hidden -n "AIza|GEMINI_API_KEY" -g '!node_modules/**' -g '!.next/**' -g '!docs/**' .
git check-ignore -v .claude/worktrees/mystifying-zhukovsky-f105dd/package.json
npm run build
npm run lint
```

期望：

- 不再出現真實 API key 形態內容
- `.claude/` 被 ignore 或已移出專案
- build 通過
- lint 不再進入互動式設定

## 七、剩餘風險

- `.env.local` 根目錄仍存在作為本機環境使用，這可接受，但必須確定永不 commit。
- 舊 key 必須 rotate；QA 無法從本機確認雲端 key 是否已停用。
- Grounding sources 與 SDK 遷移仍為上線前項目。

## 八、QA 結論

本輪 QA 判定：`Fail`。  
解除 Fail 的最低必要條件：完成 `QA3-001` 與 `QA3-002`。在這兩項完成前，不可 `git add .`、不可 push、不可公開交付。
